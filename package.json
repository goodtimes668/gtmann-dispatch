{
  "name": "gtmann-dispatch",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/clean-netlify-functions.mjs && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13",
    "@netlify/functions": "^5.3.0",
    "@netlify/identity": "^1.2.0"
  },
  "devDependencies": {
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
